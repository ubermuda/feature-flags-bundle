{% extends '@UbermudaFeatureFlags/admin/list.html.twig' %}

{# importmap() is intentionally not wired in the test kernel; override the block
   to empty so the admin base can render without calling it. #}
{% block importmap %}{% endblock %}
